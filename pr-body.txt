## Summary
- Added `./vite` package export pointing to `src/vite.js`, which re-exports `defineViewerConfig` from `src/testing/viteConfig.js`
- Kept `./testing` barrel re-exporting `defineViewerConfig` for backward compatibility (additive)
- Added a test that validates the vite entry works in plain Node context and documents why the testing barrel fails

## Test plan
- All existing tests pass (329 tests, 30 test files)
- New vite-entry test validates that:
  - `defineViewerConfig` can be imported from `./vite` in a plain Node process (no Vite/Vitest transform)
  - The testing barrel fails with `ERR_UNKNOWN_FILE_EXTENSION` on AppRoot.vue, showing why the separation was necessary

## Changes
- `src/vite.js` – new entry point that re-exports defineViewerConfig with explanatory comment
- `package.json` – added `"./vite": "./src/vite.js"` to exports
- `tests/vite-entry.test.js` – new test file validating the entry works in plain Node and documents the testing barrel failure
- `CHANGELOG.md` – documented the change for 1.13.1
- `README.md` – added example of using defineViewerConfig in vite.config.js, clarified the separate entry point
- `package.json` – bumped version to 1.13.1

🤖 Generated with [Claude Code](https://claude.com/claude-code)
